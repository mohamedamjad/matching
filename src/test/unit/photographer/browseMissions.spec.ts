describe('Browse Missions', () => {
  test('photographer can browse the missions available for them on a map. [HAPPY CASE]', async () => {});
  test('photographer can browse the missions available for them on a map. [BAD CASE: no mission available]', async () => {});
});
