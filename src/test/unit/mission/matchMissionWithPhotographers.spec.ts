describe('Match Mission With Photographers', () => {
  test('we want to notify the “best-matched” photographers for a newly created [HAPPY CASE]', async () => {});
  test('we want to notify the “best-matched” photographers for a newly created  [BAD CASE: no photographer found]', async () => {});
});
